---
/**
 * /vaporsoft/ — the console, opened on Assets.
 *
 * The files it lists stay at /brand/, where public/brand/ is copied. The page
 * moved and the files did not, which is the right way round: an address that
 * something links to is worth more than a tidy pair of names.
 */
import BrandPage from '../../components/BrandPage.astro';
---

<BrandPage tab="assets" />
